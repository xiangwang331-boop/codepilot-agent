# CodePilot 环境变量示例。复制为 .env 或在 shell 里 export。
# 注意：CodePilot 目前不自动加载 .env，请在运行前手动 export。

# 供应商 API Key（必填，真实端到端需要）
# LLM_API_KEY=sk-your-key-here

# OpenAI 兼容端点（DeepSeek/OpenAI/其他）。留空走 OpenAI 默认。
# DeepSeek 示例: https://api.deepseek.com/v1
LLM_BASE_URL=https://api.deepseek.com

# 模型名
LLM_MODEL=deepseek-v4-flash

# workspace 根目录（默认 codepilot/workspace_data）
# WORKSPACE_ROOT=

# 最大迭代次数，防死循环
# MAX_ITERATIONS=20

# ---- P4-3-2 Token-aware Context Budget（可选）----
# 模型上下文窗口 token 预算；注释掉 = 关闭 token 守卫（仅按消息数触发，行为等同 P4-3-1）。
# deepseek-v4-flash 为 1M token；填小窗口模型（如 gpt-4o-mini 128000）时成为主守卫。
# CONTEXT_LIMIT=

# token 预算外 headroom（容纳模型输出 + 下一轮新消息 + 估算误差）；注释掉 = context_limit//5。
# RESERVE_TOKENS=

# ---- P5 Docker Sandbox（可选）----
# run_command 执行宿主：local = 本机 subprocess（默认，P0–P4 行为）；docker = Docker 沙箱。
# docker 模式要求 Docker Desktop 已启动 + 镜像已构建：
#   docker build -t codepilot-sandbox:py3.12 -f Dockerfile.sandbox .
# SANDBOX_MODE=local
# SANDBOX_IMAGE=codepilot-sandbox:py3.12

# ---- P6 PostgreSQL 持久化（可选）----
# 持久化后端：sqlite = checkpoint 落 data/checkpoints.db、事件仅存内存（默认，P0–P5 行为）；
# postgres = checkpoint 与事件都落 PostgreSQL。设 postgres 前先起库：
#   docker compose up -d
# 注意：PERSISTENCE_BACKEND 与上面的 SANDBOX_MODE 是两个正交开关，互不影响。
# PERSISTENCE_BACKEND=sqlite

# postgres 后端的连接串（与 docker-compose.yml 的账号/库对齐）。
# DATABASE_URL=postgresql://codepilot:codepilot@localhost:5432/codepilot

# 跑 PG 集成测试用（tests/test_postgres_live.py）——刻意与 DATABASE_URL 分开，
# 因为那些用例会真往库里写数据，不该顺手污染你在用的库。不设则相关用例自动跳过。
# TEST_DATABASE_URL=postgresql://codepilot:codepilot@localhost:5432/codepilot
