.venv/
__pycache__/
*.pyc
.pytest_cache/
workspace_data/
data/
.env

# --- IDE ---
# JetBrains 的工程配置：里面是本机绝对路径（.idea/dbnavigator.xml 指向
# <本机>\data\checkpoints.db）、个人检查配置，对别人没有意义。
# 曾经被跟踪过 8 个文件，2026-09-23 已 git rm --cached 移出跟踪。
.idea/

# --- P8 前端 ---
# 依赖。`web/package-lock.json` **要提交**——npm 没有锁文件就是浮动版本，构建不可复现。
web/node_modules/
# TS 增量构建缓存（Vite 模板写到 node_modules/.tmp，这里兜一道防手滑）
web/*.tsbuildinfo
# React 构建产物：托管在 api/static（`api/app.py:113-114` 的 StaticFiles 一行不改）。
# 产物不进 git（Vite 文件名带 hash，提交只会制造无意义 diff），
# 所以**新克隆下 `GET /` 是 404**，先跑 `npm run build`。
#
# ⚠️ 这里**不放 `!api/static/.gitkeep`**：`emptyOutDir: true` 的 vite build 会把
# 整个 api/static 清空，包括 .gitkeep（实测），于是每次构建都多出一个「被删除」的
# 待提交变更。而 `api/app.py:113` 是 `if STATIC_DIR.is_dir():` —— 目录不存在只是
# 不挂载（`GET /` 404，REST/WS 全正常），不需要占位文件。
api/static/

# --- 内部文档：仅本地保留，不进仓库 ---
# CLAUDE.md（Claude Code 的项目记忆）与 DESIGN.md（设计文档 + 演进记录）在
# 2026-09-23 已用 filter-branch 从**全部提交**中移除（不只是最新那版）——
# 只 git rm --cached 的话历史里仍能全文翻到，等于没删。本地文件原样保留。
# README.old.md 是重写 README 时留下的旧版备份（同样是内部视角的流水账）。
CLAUDE.md
DESIGN.md
README.old.md
